#include <stdio.h>

void main()
{
	int i,j,k;
	printf("An \"escape sequence\" is a  series of characters that represents a special character.\n\t");
	getch();
	// We used \" to quote 2 of those words. 
	// \n shows it is going to start a new line instead to continue in the same line. 
	// \t will create a tab gap just like in real keyboard.
}
